# TODO: Whenever a module file is added to modules/, add the
#       import here via the below import line so we can directly
#       access all the module code in main.py via:
#         from modules import *
from modules.module import *